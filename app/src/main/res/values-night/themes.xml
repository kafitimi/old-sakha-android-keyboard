<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.OldSakhaKeyboard" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Цвет шрифта -->
        <item name="android:colorForeground">@color/font2</item>

        <!-- Цвет фона кнопок -->
        <item name="android:colorButtonNormal">@color/button2</item>

        <!-- Цвет клавиатуры -->
        <item name="android:background">@color/background2</item>

        <!-- Шрифт -->
        <item name="android:fontFamily">@font/noto_sans_display</item>

        <!-- Отключаем капс -->
        <item name="android:textAllCaps">false</item>
    </style>
</resources>