<resources xmlns:android="http://schemas.android.com/apk/res/android">
    <style name="Theme.OldSakhaKeyboard" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Цвет шрифта -->
        <item name="android:colorForeground">@color/font1</item>

        <!-- Цвет фона кнопок -->
        <item name="android:colorButtonNormal">@color/button1</item>

        <!-- Цвет клавиатуры -->
        <item name="android:background">@color/background1</item>

        <!-- Шрифт -->
        <item name="android:fontFamily">@font/noto_sans_display</item>

        <!-- Отключаем капс -->
        <item name="android:textAllCaps">false</item>
    </style>
</resources>